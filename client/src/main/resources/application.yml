spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: http://localhost:8081
          predicates:
            - Path=/users/**
okta:
  oauth2:
    issuer: https://engix-tech.us.auth0.com/
    audience: engix-taskfreela
